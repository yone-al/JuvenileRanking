# Google Apps Script API Configuration (Server-side only)
GAS_API_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# UI Timer Configuration (Client-side)
NEXT_PUBLIC_TAB_ROTATION_INTERVAL=5000    # タブローテーション間隔（ミリ秒）
NEXT_PUBLIC_TAB_STARTUP_DELAY=20000       # 起動時遅延（ミリ秒）
NEXT_PUBLIC_TAB_MANUAL_PAUSE=30000        # 手動操作後の一時停止（ミリ秒）
